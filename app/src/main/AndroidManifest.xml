<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.USE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-feature android:name="android.hardware.camera" android:required="false" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.FBLAApp.Light">
        
        <!-- Login Activity - Launcher -->
        <activity
            android:name=".Login"
            android:theme="@style/Theme.FBLAApp.NoActionBar"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        
        <!-- Register Activity -->
        <activity
            android:name=".Register"
            android:theme="@style/Theme.FBLAApp.NoActionBar"
            android:exported="false" />
        
        <!-- Main Activity (legacy, redirects to Home) -->
        <activity
            android:name=".MainActivity"
            android:exported="false" />
        
        <!-- Home Activity (Announcements/News) -->
        <activity
            android:name=".HomeActivity"
            android:exported="false" />
        
        <!-- Profile Activity -->
        <activity
            android:name=".ProfileActivity"
            android:exported="false" />
        
        <!-- Calendar/Events Activity -->
        <activity
            android:name=".CalendarActivity"
            android:exported="false" />
        
        <!-- Resources Activity -->
        <activity
            android:name=".ResourcesActivity"
            android:exported="false" />
        
        <!-- Social Activity -->
        <activity
            android:name=".SocialActivity"
            android:exported="false" />
        
        <!-- Add/Edit Event Activity -->
        <activity
            android:name=".AddEditEventActivity"
            android:theme="@style/Theme.FBLAApp.FloatingDialog"
            android:exported="false" />
        
        <!-- Event Detail Activity -->
        <activity
            android:name=".EventDetailActivity"
            android:exported="false" />
        
        <!-- Reminder Broadcast Receiver -->
        <receiver
            android:name=".ReminderBroadcastReceiver"
            android:exported="false" />
        
        <!-- FileProvider for camera -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>
            
    </application>

</manifest>
